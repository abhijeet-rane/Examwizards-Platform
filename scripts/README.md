# Scripts

| Script | Description |
|--------|-------------|
| [`../deploy/scripts/push-ecr.example.sh`](../deploy/scripts/push-ecr.example.sh) | Tag and push the API image to Amazon ECR. |

Add small automation here (database seed, local TLS, etc.) as your workflow matures. Keep secrets out of scripts; use environment variables or your CI secret store.
